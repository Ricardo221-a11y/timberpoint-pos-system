import React,{useEffect,useMemo,useState}from'react';import{createRoot}from'react-dom/client';import{BrowserRouter,Routes,Route,NavLink,Navigate,Outlet}from'react-router-dom';import{api,login,tk}from'./api';import'./styles.css';
function Layout(){return <div className="layout"><aside className="side"><div className="logo">TIMBER<b>POINT</b><small style={{display:'block'}}>SMART POS</small></div><nav className="nav"><NavLink to="/">Point of Sale</NavLink><NavLink to="/dashboard">Dashboard</NavLink><NavLink to="/stock">Stock</NavLink><NavLink to="/sales">Sales</NavLink><NavLink to="/customers">Debtors</NavLink><NavLink to="/suppliers">Creditors</NavLink><a href="#" onClick={()=>{localStorage.clear();location.href='/login'}}>Sign out</a></nav></aside><main className="main"><Outlet/></main></div>}
function Login(){let[e,se]=useState('director@timberdemo.co.zw'),[p,sp]=useState('TimberPOS2026!'),[er,sr]=useState('');return <div className="login"><form className="loginbox" onSubmit={async x=>{x.preventDefault();try{await login(e,p);location.href='/'}catch(z){sr(z.message)}}}><h1>TimberPoint POS</h1><p>Sales, stock, profit and customer accounts.</p><input value={e} onChange={x=>se(x.target.value)}/><input type="password" value={p} onChange={x=>sp(x.target.value)}/>{er&&<p>{er}</p>}<button className="btn" style={{width:'100%'}}>Sign in</button></form></div>}
function POS(){let[products,setProducts]=useState([]),[sites,setSites]=useState([]),[customers,setCustomers]=useState([]),[site,setSite]=useState(''),[cust,setCust]=useState(''),[level,setLevel]=useState('retail'),[currency,setCurrency]=useState('USD'),[rate,setRate]=useState(1),[search,setSearch]=useState(''),[cart,setCart]=useState([]),[paid,setPaid]=useState(0),[method,setMethod]=useState('cash'),[last,setLast]=useState(null),[msg,setMsg]=useState('');let load=()=>Promise.all([api('/api/products'+(site?`?site_id=${site}`:'')),api('/api/sites'),api('/api/customers')]).then(([p,s,c])=>{setProducts(p);setSites(s);setCustomers(c);if(!site&&s[0])setSite(String(s[0].id))});useEffect(()=>{load()},[site]);let shown=products.filter(x=>(x.name+x.sku+x.barcode).toLowerCase().includes(search.toLowerCase()));let total=cart.reduce((a,x)=>a+x.quantity*x.unit_price,0);function add(p){setCart(c=>{let z=c.find(x=>x.product_id===p.id);return z?c.map(x=>x.product_id===p.id?{...x,quantity:x.quantity+1}:x):[...c,{product_id:p.id,name:p.name,sku:p.sku,quantity:1,unit_price:p[level]}]})}async function complete(){let payload={number:'SALE-'+Date.now(),site_id:Number(site),customer_id:cust?Number(cust):null,currency,exchange_rate:Number(rate),price_level:level,lines:cart,discount:0,payment_method:method,amount_paid:Number(paid)};try{let s=navigator.onLine?await api('/api/sales',{method:'POST',body:JSON.stringify(payload)}):null;if(!navigator.onLine){let q=JSON.parse(localStorage.getItem('offlineSales')||'[]');q.push(payload);localStorage.setItem('offlineSales',JSON.stringify(q));setMsg('Sale queued offline')}else{setLast(s);setMsg('Sale completed')}setCart([]);setPaid(0);load()}catch(e){setMsg(e.message)}}async function sync(){let q=JSON.parse(localStorage.getItem('offlineSales')||'[]');for(let s of q)await api('/api/sales',{method:'POST',body:JSON.stringify(s)});localStorage.removeItem('offlineSales');setMsg('Offline sales synchronized');load()}return <><div className="top"><div><h1>Timber Point of Sale</h1><div>Scan barcode or select timber product</div></div><div className="actions"><button className="btn alt" onClick={sync}>Sync offline sales</button></div></div>{!navigator.onLine&&<div className="offline">Offline mode: completed sales will be queued.</div>}{msg&&<div className="card">{msg}</div>}<div className="pos"><div><div className="card"><div className="actions"><input style={{flex:1,padding:10}} autoFocus value={search} onChange={e=>setSearch(e.target.value)} placeholder="Scan barcode, SKU or search timber"/><select value={site} onChange={e=>setSite(e.target.value)}>{sites.map(x=><option value={x.id}>{x.name}</option>)}</select><select value={level} onChange={e=>setLevel(e.target.value)}><option value="retail">Retail</option><option value="contractor">Contractor</option><option value="bulk">Bulk</option></select></div></div><div className="products">{shown.map(p=><button className="product" onClick={()=>add(p)}><strong>{p.name}</strong><small>{p.dimensions} · Stock {p.quantity}</small><h3>${p[level]}</h3></button>)}</div></div><div className="card"><h2>Sale</h2>{cart.map(x=><div className="cartline"><span>{x.name}</span><input type="number" min="1" value={x.quantity} onChange={e=>setCart(c=>c.map(y=>y.product_id===x.product_id?{...y,quantity:Number(e.target.value)}:y))}/><span>${(x.quantity*x.unit_price).toFixed(2)}</span><button onClick={()=>setCart(c=>c.filter(y=>y.product_id!==x.product_id))}>×</button></div>)}<h2>Total: {currency} {(total*rate).toFixed(2)}</h2><div className="field"><label>Customer</label><select value={cust} onChange={e=>setCust(e.target.value)}><option value="">Walk-in</option>{customers.map(x=><option value={x.id}>{x.name}</option>)}</select></div><div className="actions"><select value={currency} onChange={e=>setCurrency(e.target.value)}><option>USD</option><option>ZiG</option></select><input type="number" value={rate} onChange={e=>setRate(e.target.value)} title="Exchange rate"/><select value={method} onChange={e=>setMethod(e.target.value)}><option>cash</option><option>card</option><option>mobile_money</option><option>credit</option></select></div><div className="field"><label>Amount paid</label><input type="number" value={paid} onChange={e=>setPaid(e.target.value)}/></div><button className="btn" disabled={!cart.length} style={{width:'100%'}} onClick={complete}>Charge {currency} {(total*rate).toFixed(2)}</button>{last&&<div className="receipt"><hr/><h3>TIMBERPOINT</h3><div>{last.number}</div>{last.lines.map(x=><div>{x.name} x{x.quantity} {x.total.toFixed(2)}</div>)}<b>Total {last.currency} {last.total.toFixed(2)}</b><p>Reliable timber · Lasting strength</p><button className="btn alt" onClick={()=>print()}>Print receipt</button></div>}</div></div></>}
function Dashboard(){let[d,s]=useState({});useEffect(()=>{api('/api/dashboard').then(s)},[]);return <><div className="top"><h1>Business Dashboard</h1></div><div className="grid">{[['Revenue',d.revenue],['Gross profit',d.gross_profit],['Stock value',d.stock_value],['Low stock',d.low_stock],['Debtors',d.debtors],['Creditors',d.creditors]].map(([a,b])=><div className="card stat"><span>{a}</span><strong>{a==='Low stock'?b:'$'+Number(b||0).toFixed(2)}</strong></div>)}</div></>}
function TablePage({title,path}){let[r,s]=useState([]);useEffect(()=>{api(path).then(s)},[path]);return <><div className="top"><h1>{title}</h1></div><div className="card"><table className="table"><thead><tr><th>Reference</th><th>Name / Details</th><th>Amount / Stock</th></tr></thead><tbody>{r.map(x=><tr><td>{x.number||x.code||x.sku}</td><td>{x.name||x.payment_method||x.category}</td><td>{x.total??x.balance??x.quantity}</td></tr>)}</tbody></table></div></>}
let G=({children})=>tk()?children:<Navigate to="/login"/>;createRoot(document.getElementById('root')).render(<BrowserRouter><Routes><Route path="/login" element={<Login/>}/><Route element={<G><Layout/></G>}><Route path="/" element={<POS/>}/><Route path="/dashboard" element={<Dashboard/>}/><Route path="/stock" element={<TablePage title="Stock" path="/api/products"/>}/><Route path="/sales" element={<TablePage title="Sales" path="/api/sales"/>}/><Route path="/customers" element={<TablePage title="Customers & Debtors" path="/api/customers"/>}/><Route path="/suppliers" element={<TablePage title="Suppliers & Creditors" path="/api/suppliers"/>}/></Route></Routes></BrowserRouter>);
